# ZAP scan configuration for authenticated testing
# Enables both passive and active scanning when running zap-cli or zap-* scripts
scan.mode=attack
scanners.passive=true
scanners.active=true
spider.maxDepth=5
scan.policy=Medium
fail.on.high=true
